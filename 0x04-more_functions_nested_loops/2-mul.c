#include <stdio.h>

#include "main.h"

/**
 * mul - multiplies two integers.
 * @a: the number to be multiplied.
 * @b: second number.
 * Return: Always a * b the product.
 */

int mul(int a, int b)
{
	return (a * b);
}
